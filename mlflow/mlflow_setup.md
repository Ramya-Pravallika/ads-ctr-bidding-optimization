# MLflow Setup

To start MLflow for experiment tracking:
1. Run `mlflow ui` in root directory (or inside Docker).
2. MLflow server will be accessible at `http://localhost:5000`.
3. Models and experiments from Airflow DAGs will be logged here.

More info: https://mlflow.org/docs/latest/index.html
